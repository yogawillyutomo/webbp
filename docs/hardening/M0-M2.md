# WEBBP M0–M2 Hardening Record

## Scope

Branch: `feat/m0-m2-hardening`

Baseline commit: `04f4c766d7dcdef596665bcf2cb28525e14d2dad`

This tranche covers:

- M0 — Baseline & Safety Gate
- M1 — Security & Dependency Hardening
- M2 — Production Trust Cleanup

## M0 — Baseline

Observed repository state before changes:

- Default branch: `main`
- Baseline deployment status: Vercel success
- GitHub Actions workflows: none observed
- Pull requests: none observed
- Branch protection: not enabled
- Runtime framework: Next.js 16.1.6
- Placeholder WhatsApp and social links existed in production source
- Privacy Policy and Terms of Service links pointed to `#`

Working rule for this tranche:

- No direct changes to `main`
- No merge from this tranche without an explicit merge decision
- Do not invent unverified contact channels
- Preserve reproducible npm lock state

## M1 — Security & Dependency Hardening

Implemented:

- Next.js pinned from 16.1.6 to 16.3.3
- `package-lock.json` runtime graph synchronized to Next.js 16.3.3
- Added `npm run verify` = lint + production build

The ESLint config package remains at 16.1.6 in this tranche to avoid unrelated lint-stack churn. Runtime Next.js is the security-critical dependency being patched here.

## M2 — Production Trust Cleanup

Implemented:

- Added centralized `src/config/site.js`
- Removed placeholder WhatsApp number
- Removed unverified X, LinkedIn, Instagram, and WhatsApp footer links
- Added verified Bakaran Project GitHub organization link
- Reused the website's existing public contact email
- Removed dead `href="#"` service/footer links
- Added real `/privacy` page
- Added real `/terms` page
- Updated contact/footer copy to avoid unsupported claims and placeholder channels

## Verification

Required before merge:

```bash
npm ci
npm run verify
```

Also verify the Vercel preview deployment and manually check:

- Home
- Contact email CTA
- GitHub organization link
- Privacy Policy
- Terms of Service
- No placeholder WhatsApp number or generic social homepages remain
